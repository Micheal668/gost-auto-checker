# 📘 GOST Academic Report Checker

## Система автоматической проверки академических отчетов по ГОСТ

Данный проект представляет собой **веб-приложение для проверки оформления академических отчетов в соответствии с требованиями российских государственных стандартов (ГОСТ)**.  
Пользователь загружает файл формата **`.docx`**, после чего система выполняет автоматическую проверку структуры и оформления документа и формирует отчет с найденными несоответствиями.

Проект реализован как **минимально жизнеспособная версия (MVP)** и предназначен для учебных и академических целей.

---

## 1. Общая архитектура проекта

```text
Frontend (Vue 3 + Vite)
        ↓ HTTP API
Backend (Django + Django REST Framework)
        ↓
Celery Worker (асинхронная обработка)
        ↓
Rule Engine (runtime.json, скомпилированный из DSL)
        ↓
DOCX-файл с результатами проверки
2. Поддерживаемые технологии

Backend: Python 3.12, Django, Celery, Redis

Frontend: Vue 3, Vite, JavaScript, CSS

Формат документов: только .docx

База данных: SQLite (по умолчанию), MySQL (опционально)

3. Структура проекта
gost-mvp/
├── backend/
│   ├── apps/
│   │   ├── jobs/            # Задачи проверки (создание, статус, скачивание)
│   │   └── checker/         # Движок проверки, правила ГОСТ, DSL
│   ├── config/              # Django и Celery конфигурация
│   ├── requirements.txt     # Python зависимости
│   └── venv/                # Виртуальное окружение (Python 3.12)
├── frontend/
│   ├── src/                 # Исходный код Vue
│   ├── package.json
│   └── vite.config.js
└── get-start.sh              # Скрипт запуска (macOS / Linux)

4. Запуск проекта на macOS / Linux
4.1 Требования

macOS или Linux

Homebrew (для macOS)

Git

Доступ к интернету (для установки зависимостей)

4.2 Быстрый запуск (рекомендуется)
chmod +x get-start.sh
./get-start.sh


Скрипт автоматически выполняет:

установку Python 3.12 (через Homebrew),

создание и проверку виртуального окружения,

установку backend-зависимостей (инкрементно),

компиляцию DSL-правил ГОСТ в runtime.json,

запуск Redis, Celery и Django,

запуск frontend (Vite),

открытие приложения в браузере.

После запуска:

Frontend: http://localhost:5173

Backend API: http://127.0.0.1:8000/api/

5. Запуск проекта на Windows

⚠️ Windows не поддерживает запуск get-start.sh напрямую.

5.1 Рекомендуемый порядок действий (PowerShell)
1. Установить Python 3.12 (официальный сайт python.org)
2. Создать виртуальное окружение
cd backend
py -3.12 -m venv venv
venv\Scripts\activate

3. Установить зависимости
pip install -r requirements.txt

4. Скомпилировать правила ГОСТ (DSL → runtime)
python apps\checker\engine\compile_dsl.py ^
  apps\checker\standards\gost_7_32_2017.yaml ^
  apps\checker\standards\gost_7_32_2017.runtime.json

5. Выполнить миграции и запустить backend
python manage.py migrate
python manage.py runserver

6. Запустить Celery (в новом окне терминала)
celery -A config.celery_app worker -l info -P solo

7. Запустить frontend
cd frontend
npm install
npm run dev

6. Где изменить путь к проекту

В файле get-start.sh в начале скрипта необходимо изменить следующие строки:

PROJECT_ROOT="/абсолютный/путь/к/проекту/gost-mvp"
BACKEND_DIR="$PROJECT_ROOT/backend"
FRONTEND_DIR="$PROJECT_ROOT/frontend"


⚠️ Используйте абсолютные пути, а не относительные.

7. База данных
7.1 SQLite (по умолчанию)

Используется автоматически

Не требует настройки

Рекомендуется для MVP, учебных проектов и локального запуска

7.2 MySQL (опционально)

Перед запуском установите переменные окружения:

export USE_MYSQL=1
export DB_NAME=gost_checker
export DB_USER=gost
export DB_PASSWORD=StrongPass123!
export DB_HOST=127.0.0.1
export DB_PORT=3306


После этого Django автоматически переключится на MySQL.

8. Ограничения и особенности

Поддерживается только формат .docx

Другие форматы (.pdf, .doc, .odt) будут отклонены

Проверка выполняется асинхронно (через Celery)

AI-проверка (GPT / DeepSeek / Qwen) предусмотрена архитектурно, но может быть отключена в MVP

9. API (кратко)

POST /api/jobs/ — загрузка документа и создание задачи

GET /api/jobs/{id}/ — получение статуса и прогресса

GET /api/jobs/{id}/download/ — скачивание отчета о проверке

10. Примечания

Python 3.12 обязателен

Правила ГОСТ описываются в DSL (YAML) и компилируются в runtime JSON

Архитектура проекта рассчитана на расширение (новые ГОСТ, AI-модули, языки)